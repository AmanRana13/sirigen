import {makeStyles} from 'tss-react/mui';

export const headerWrapperStyle = makeStyles()((theme) => ({
  navigationTabs: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
  },
}));
