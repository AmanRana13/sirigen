import {makeStyles} from 'tss-react/mui';

export const medicalInfoDetailStyle = makeStyles()(() => ({
  container: {
    width: '100%',
    backgroundColor: '#fff',
    borderRadius: 10,
    padding: 10,
  },
}));
