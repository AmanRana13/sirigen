import {makeStyles} from 'tss-react/mui';

export const seniorCallSchedulerStyle = makeStyles()(() => ({
  container: {
    //margin: '10px 5%',
    marginBottom: 50,
    fontSize: 15,
  },
  heading: {
    fontWeight: 'bold',
    fontSize: 20,
    marginBottom: 4,
  },
}));
